# [[할일앱]]-세션 로그 - 2026-06-03 오후

## 이번 세션에서 한 일
- Xcode Command Line Tools 설치 (Node.js 설치 전 선행 작업)
- nvm 설치 후 Node.js v24.16.0 설치
- Vite + React 프로젝트 생성 (`todo-app/`)
- Vite 보일러플레이트 제거 및 아키텍처 설계대로 파일 구조 정리
  - `src/components/TodoInput.jsx`, `TodoList.jsx`, `TodoItem.jsx` 생성
  - `src/assets/` 제거, `App.jsx` / `App.css` 초기화
- Phase 2 핵심 기능 구현
  - `App.jsx`: `todos` 상태, `handleAdd` / `handleToggle` / `handleDelete` 함수
  - `TodoInput.jsx`: 입력창 + 추가 버튼, 빈 입력 방지
  - `TodoList.jsx`: `todos` 배열을 `map`으로 렌더링
  - `TodoItem.jsx`: 체크박스(완료 토글), 취소선, 삭제 버튼
- 개발 서버 실행 확인 (http://localhost:5173)

## 발생한 문제
- `npm create vite@latest .` 실행 시 `.claude` 폴더가 있어 "디렉토리가 비어있지 않음"으로 취소됨
- Node.js / npm이 설치되어 있지 않아 처음부터 환경 셋업 필요

## 해결한 방법
- `--overwrite` 옵션으로 Vite 프로젝트 생성 재시도
- Xcode CLT → nvm → Node.js 순서로 설치

## 다음 세션에서 할 일
- Phase 2 동작 브라우저 검증 (추가 / 토글 / 삭제 직접 확인)
- Phase 3: localStorage 연동 구현
  - `useState` 초기값에 localStorage 읽기
  - `useEffect`로 자동 저장
  - 새로고침 후 데이터 유지 확인

## 현재 프로젝트 상태
- 체크리스트 기준: Phase 2의 마지막 항목(할일 삭제)까지 완료
- Phase 1 ✅ / Phase 2 ✅ / Phase 3 ⬜ / Phase 4 ⬜

---

# [[할일앱]]-세션 로그 - 2026-06-03 저녁

## 이번 세션에서 한 일
- Phase 3 localStorage 연동 구현
  - `useState` 초기값에 `localStorage.getItem('todos')` 읽기 추가
  - `useEffect`로 `todos` 변경 시 자동 저장
- Phase 3.5 기능 확장 (마감일 / 카테고리 / 필터)
  - `CATEGORIES` 상수 정의 (업무/개인/학습/건강 + 색상)
  - `TodoInput.jsx`: 카테고리 `<select>`, 마감일 `<input type="date">` 추가
  - `TodoItem.jsx`: 카테고리 색상 뱃지, 마감일 표시, 기한 경과 시 ⚠️ + 빨간 테두리
  - `App.jsx`: 카테고리별 / 완료 여부별 필터 state + 필터 UI
  - `App.css`: 전체 UI 스타일 작성 (기존 비어있었음)
- Obsidian 문서 업데이트 (체크리스트, 프로젝트-개요, 아키텍처)

## 발생한 문제
- 없음

## 해결한 방법
- 해당 없음

## 다음 세션에서 할 일
- 브라우저에서 전체 동작 검증
  - localStorage 새로고침 유지 확인
  - 마감일 기한 경과 강조 확인
  - 카테고리 필터 / 상태 필터 동작 확인
- Vercel 배포 (Phase 4)

## 현재 프로젝트 상태
- 체크리스트 기준: Phase 3.5의 마지막 항목(UI 스타일)까지 완료
- Phase 1 ✅ / Phase 2 ✅ / Phase 3 ✅ / Phase 3.5 ✅ / Phase 4 ⬜
